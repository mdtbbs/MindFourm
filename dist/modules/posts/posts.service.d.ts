import { Repository, DataSource } from 'typeorm';
import { Post } from '@entities/post.entity';
import { User } from '@entities/user.entity';
import { Category } from '@entities/category.entity';
import { Tag } from '@entities/tag.entity';
import { PostTag } from '@entities/post-tag.entity';
import { Reply } from '@entities/reply.entity';
import { RedisService } from '../../database/redis.service';
import { PointsService } from '../points/points.service';
import { GroupsService } from '../groups/groups.service';
import { EventBusService } from '../plugins/event-bus.service';
import { NotificationsService } from '../notifications/notifications.service';
import { CreatePostDto } from './dto/create-post.dto';
import { UpdatePostDto } from './dto/update-post.dto';
import { QueryPostsDto } from './dto/query-posts.dto';
export declare class PostsService {
    private postRepository;
    private userRepository;
    private categoryRepository;
    private tagRepository;
    private postTagRepository;
    private replyRepository;
    private dataSource;
    private redisService;
    private pointsService;
    private groupsService;
    private eventBus;
    private notificationsService;
    constructor(postRepository: Repository<Post>, userRepository: Repository<User>, categoryRepository: Repository<Category>, tagRepository: Repository<Tag>, postTagRepository: Repository<PostTag>, replyRepository: Repository<Reply>, dataSource: DataSource, redisService: RedisService, pointsService: PointsService, groupsService: GroupsService, eventBus: EventBusService, notificationsService: NotificationsService);
    create(dto: CreatePostDto, userId: number): Promise<Post | null>;
    findById(id: number, userId?: number): Promise<Post>;
    findAll(query: QueryPostsDto): Promise<{
        data: Post[];
        total: number;
        page: number;
        limit: number;
        totalPages: number;
    }>;
    findAllCursor(query: QueryPostsDto): Promise<{
        data: Post[];
        nextCursor: string | null;
        hasMore: boolean;
    }>;
    update(id: number, dto: UpdatePostDto, userId: number, userRole: string): Promise<Post | null>;
    softDelete(id: number, userId: number, userRole: string): Promise<void>;
    hardDelete(id: number): Promise<void>;
    incrementViewCount(id: number): Promise<void>;
    pin(id: number, isPinned: number): Promise<Post>;
    move(id: number, categoryId: number): Promise<Post>;
    getReplyCount(postId: number): Promise<number>;
    getReplies(postId: number, limit?: number, page?: number): Promise<{
        data: Reply[];
        total: number;
        page: number;
        limit: number;
        totalPages: number;
    }>;
    private attachTags;
    private invalidatePostCache;
    search(query: string, limit?: number): Promise<Post[]>;
    findByUser(userId: number, page?: number, limit?: number): Promise<{
        data: Post[];
        total: number;
        page: number;
        limit: number;
        totalPages: number;
    }>;
    getTrending(limit?: number): Promise<Post[]>;
    getPinned(categoryId?: number): Promise<Post[]>;
}
